{
  "name": "backend",
  "version": "1.0.0",
  "description": "- Install all dependency\r ```\r npm install\r ```\r - Start the backend\r ```\r npm start\r ```\r - backend started on ```http://localhost:4000```\r ## Routes\r - ```/uid``` for user profile data",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "mongoose": "^9.0.1"
  }
}
